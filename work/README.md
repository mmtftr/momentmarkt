# work/ — run-time artifacts

All artifacts produced by stage runs land here. Agents own specific files;
see ../ARTIFACTS.md for the contract.

Sub-directories created on first use:
- explore/   explorer reports (one per question)
- history/   versioned previous artifacts
- scratch/   explorer-only, nobody else reads these
